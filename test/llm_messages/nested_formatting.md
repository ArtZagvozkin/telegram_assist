Вот текст, демонстрирующий различные вложенные форматы:

Это **основной жирный текст**, и внутри него мы видим *вложенный курсивный текст*. В этом *курсивном тексте* можно найти **еще более жирный фрагмент** для дополнительного акцента.

Продолжая эту мысль, рассмотрим маркированный список, где каждый пункт содержит свой собственный набор форматирования.

*   Пункт 1: Здесь мы используем `однострочный код` для выделения ключевого термина.
*   Пункт 2: Внутри этого пункта, мы можем вложить **жирный текст**, который, в свою очередь, содержит *курсив*.
    *   Подпункт 2.1: Это более глубокий уровень вложенности. В нем мы видим, как *жирный и курсив* комбинируются: ***очень жирный курсив***.
*   Пункт 3: Для демонстрации следующего примера, мы разместим блок кода внутри этого пункта.

```
// Блок кода:
function example() {
    // Внутри этого блока, мы можем использовать форматирование,
    // но обычно разметка игнорируется. Однако, для целей демонстрации:
    console.log("Вложенный *текст* в блоке `кода`.");
}
```

В завершение, обратите внимание на этот сложный пример: **текст, где *курсив* охватывает `блок кода` и заканчивается жирным шрифтом**.